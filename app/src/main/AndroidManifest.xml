<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.nsx.cnwinchart">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.CombinedChartActivity"/>
        <activity android:name=".activity.LineChartActivity"/>
        <activity android:name=".activity.DynamicLineChartActivity"/>
        <activity android:name=".activity.BarChartActivity"/>
        <activity android:name=".activity.RadarChartActivity"/>
        <activity android:name=".activity.PieChartActivity"/>
        <activity android:name=".activity.MeterActivity"/>
    </application>

</manifest>